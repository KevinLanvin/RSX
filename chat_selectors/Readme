Compilation : make
Execution 	: java Server

OU

./one_million_bytes

Cela va compiler , puis exécuter le serveur et envoyer une série de commandes via telnet (préparées avec expect) pour générer un graphe de temps d'utilisation CPU en fonction du nombre d'octets envoyés
Tous les fichiers indésirables sont supprimés. A la fin de l'opération, seul le graph reste.

make clean supprime les fichiers .class, les fichiers temporaires, et le graph.


Fonctionnement : 
	- Un seul selector
	- Les channels des clients ont un attachment qui contient un buffer.
	- Lors de la lecture, les octets lus sont recopiés dans les attachments de tous les clients.
	- L'écriture vide juste le buffer associé au channel du client concerné.
	- La réception d'une commande de la part d'un channel crée une Entry pour celui-ci, et ajoute l'entry à la liste correspondant à la commande choisie.
	- Si un channel qui a lancé une commande est libre en lecture ou en écriture, il déclenche un read ou un write spécial qui effectue le comportement choisi par la commande tapée en premier lieu.
	- Une fois la commande achevée, un message est envoyé et l'entry concernant le channel est supprimée. Le channel redevient un utilisateur normal.
	
